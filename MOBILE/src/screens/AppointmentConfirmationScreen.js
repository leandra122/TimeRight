import { Text } from 'react-native'; import Screen from '../components/Screen'; import { Button, Title } from '../components/UI'; import { dateTime } from '../utils/format';
export default function AppointmentConfirmationScreen({ route, navigation }) { const { appointment } = route.params; return <Screen contentStyle={{ justifyContent: 'center', flexGrow: 1 }}><Title subtitle="Seu agendamento foi registrado no TimeRight.">Tudo certo!</Title><Text style={{ fontSize: 18, marginBottom: 20 }}>{appointment.salao?.nome}{'\n'}{appointment.servico?.nome}{'\n'}{dateTime(appointment.dataHora)}</Text><Button title="Ver minha agenda" onPress={() => navigation.navigate('Main', { screen: 'Agenda' })} /><Button title="Voltar ao início" secondary onPress={() => navigation.navigate('Main', { screen: 'Home' })} /></Screen>; }
