import { Alert, Text } from 'react-native'; import Screen from '../components/Screen'; import { Button, Card, Title } from '../components/UI'; import { useAuth } from '../context/AuthContext';
export default function ProfileScreen() { const { session, logout } = useAuth(); const exit = () => Alert.alert('Sair da conta?', 'Sua sessão será removida deste dispositivo.', [{ text: 'Cancelar', style: 'cancel' }, { text: 'Sair', style: 'destructive', onPress: logout }]); return <Screen><Title>Perfil</Title><Card><Text style={{ fontSize: 19, fontWeight: '800' }}>{session.user.nome}</Text><Text>{session.user.email}</Text><Text>Perfil: Cliente</Text></Card><Button title="Atualizar tela" secondary onPress={() => {}} /><Card><Text style={{ fontWeight: '800' }}>Sobre o TimeRight</Text><Text>Aplicativo para consultar estabelecimentos e gerenciar seus próprios agendamentos.</Text></Card><Button title="Sair da conta" onPress={exit} /></Screen>; }
