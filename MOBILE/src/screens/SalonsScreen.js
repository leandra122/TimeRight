import Screen from '../components/Screen'; import { StateMessage, Title } from '../components/UI'; import SalonCard from '../components/SalonCard'; import { useSalons } from '../hooks/useCatalog';
export default function SalonsScreen({ navigation }) { const { salons, loading, error, reload } = useSalons(); return <Screen><Title subtitle="Dados reais fornecidos pelos estabelecimentos.">Salões</Title><StateMessage loading={loading} error={error} onRetry={reload} empty={!loading && !error && !salons.length ? 'Nenhum salão ativo disponível.' : null} />{salons.map((s) => <SalonCard key={s.id} salon={s} onPress={() => navigation.getParent()?.navigate('SalonDetails', { salonId: s.id })} />)}</Screen>; }
