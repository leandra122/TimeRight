server:
  port: 8080
  address: 0.0.0.0

spring:
  datasource:
    url: jdbc:h2:mem:timeright
    driverClassName: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  
  h2:
    console:
      enabled: true
      path: /h2-console
  
  mail:
    host: smtp.office365.com
    port: 587
    username: ${MAIL_USERNAME:rm94877@estudante.fieb.edu.br}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.office365.com

jwt:
  secret: ${JWT_SECRET:timeright_super_secret_jwt_key_2024}
  expiration: 86400000

cors:
  allowed-origins: http://localhost:5173